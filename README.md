# Трекер задач сотрудников
***

## Описание

"Трекер задач сотрудников" - это ***DRF*** проект.

### Функциональные возможности

+ Создание, просмотр, редактирование и удаление задач. Назначение исполнителей.<br> 
Связь задач между собой
+ Регистрация, авторизация, просмотр, редактирование и удаление пользователей.
+ Просмотр загружености исполнителей.
+ Просмотр "горящих" задач.

## Структура проекта

tasks_traker/<br>
├── manage.py # Точка входа<br>
├── pyproject.toml # Зависимости (Poetry)<br>
├── README.md # Документация<br>
│<br>
├── tasks_traker/ # Основные настройки проекта<br>
│ ├── init.py<br>
│ ├── settings.py # Настройки Django<br>
│ ├── urls.py # Главные URL маршруты<br>
│ ├── asgi.py # ASGI конфигурация<br>
│ └── wsgi.py # WSGI конфигурация<br>
│<br>
├── users/ # Приложение пользователей<br>
│ ├── init.py<br>
│ ├── admin.py # Админ-панель для пользователей<br>
│ ├── models.py # Модель CustomUser<br>
│ ├── serializers.py # Сериализаторы для пользователей<br>
│ ├── views.py # Представления (APIView, ViewSet)<br>
│ ├── urls.py # URL маршруты для users<br>
│ ├── tests.py # Тесты для users<br>
│ └── management/ # Кастомные команды<br>
│<br>
├── task/ # Приложение задач<br>
│ ├── init.py<br>
│ ├── admin.py # Админ-панель для задач<br>
│ ├── models.py # Модель Task<br>
│ ├── serializers.py # Сериализаторы для задач<br>
│ ├── views.py # ViewSet для задач<br>
│ ├── permissions.py # Кастомные permissions для задач<br>
│ ├── paginators.py # Пагинаторы<br>
│ ├── urls.py # URL маршруты для task<br>
│ ├── tests.py # Тесты для задач<br>
│ └── management/ # Кастомные команды<br>
│ └── commands/<br>
│ ├── init_data.py # Инициализация данных<br>
│ └── reset_data.py # Сброс данных<br>

# Запуск проекта

## Установка зависимостей проекта

В проекте используется виртуальное окружение ***poetry***.<br> Для установки зависимостей используйте команду 
```
poetry install
```

## Локальный запуск приложения

Локальный запуск приложения осуществляется через модуль ***manage.py*** в корне проекта.<br>
Используйте команду
```
python manage.py runserver
```

после перейдите по ссылке [task_traker][1]

[1]: http://127.0.0.1:8000/ "Ссылка на страницу приложения"

# Кастомные команды

## init_data - Наполнение базы данными

### Базовое использование<br> (создаст 5 пользователей и 10 задач)

```
python manage.py init_data
```

### С указанием количества пользователей и задач

```
python manage.py init_data --users-count 10 --tasks-count 20
```

### С очисткой существующих данных

```
python manage.py init_data --clear
```

### Комбинация параметров

```
python manage.py init_data --users-count 8 --tasks-count 15 --clear
```

## reset_data - Очистка данных

### Оставить суперпользователя

```
python manage.py reset_data
```

### Удалить суперпользователя

```
python manage.py reset_data --delete-superuser
```

# Docker-compose упаковка приложения
 + Для упаковки проекта в контейнеры Docker, и его последующего запуска, воспользуйтесь командой
```
docker-compose up -d --build
```

 + Для просмотра запущенных контейнеров

``` 
docker-compose ps
```

 + Для проверки логов контейнера

``` 
docker-compose logs -f <Имя контейнера>
```


## Описание API 

### Доступные API запросы и возможные ответы можно посмотреть тут:

[API документация][2]

[API документация][3]

[2]: http://localhost:8000/redoc/ "Ссылка на страницу c API документацией"
[3]: http://localhost:8000/swagger/ "Ссылка на страницу c API документацией"
